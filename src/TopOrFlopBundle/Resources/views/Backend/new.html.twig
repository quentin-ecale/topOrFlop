{% extends "TopOrFlopBundle::admin_layout.html.twig" %}

{% block content %}
    <h2>Nouveau média</h2>

    <form action="{{ path('admin_new') }}" method="POST">
        {{ form_row(form.title) }}
        {{ form_row(form.url) }}
        {{ form_rest(form) }}
        <input type="submit" value="Valider" />
    </form>
{% endblock %}